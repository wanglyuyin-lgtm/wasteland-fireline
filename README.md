# 废土火线

这是当前完整版《废土火线》的 GitHub Pages 发布工程。

把本文件夹的全部内容上传到 GitHub 仓库的 `main` 分支后，GitHub Actions 会自动使用 Pygbag 构建网页版，并部署到 GitHub Pages。

仓库设置中需要把 **Settings → Pages → Source** 设为 **GitHub Actions**。

发布完成后的地址格式：

`https://你的GitHub用户名.github.io/仓库名/`

第一次构建通常需要几分钟，第一次打开网页还需要下载浏览器版 Python 和 Pygame 运行环境。
